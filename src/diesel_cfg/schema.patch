diff --git a/src/diesel_cfg/schema.rs b/src/diesel_cfg/schema.rs
index 0c80b2c..5fa7273 100644
--- a/src/diesel_cfg/schema.rs
+++ b/src/diesel_cfg/schema.rs
@@ -22,12 +22,14 @@ table! {
         /// (Automatically generated by Diesel.)
         url -> Nullable<Varchar>,
     }
 }
 
 table! {
+    use diesel::sql_types::*;
+    use diesel_geometry::sql_types::Point;
     /// Representation of the `identifications` table.
     ///
     /// (Automatically generated by Diesel.)
     identifications (id) {
         /// The `id` column of the `identifications` table.
         ///
@@ -333,13 +335,7 @@ table! {
 }
 
 joinable!(avatars -> users (user_id));
 joinable!(identifications -> users (posted_by));
 joinable!(profiles -> users (user_id));
 
allow_tables_to_appear_in_same_query!(
    avatars,
    identifications,
    oath_users,
    profiles,
    users,
);
